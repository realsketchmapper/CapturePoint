export const STORAGE_KEYS = {
  USER_CREDENTIALS: '@user_credentials',
  COLLECTED_POINTS: '@collected_points',
  COLLECTED_LINES: '@collected_lines',
  UNSYNCED_POINTS: '@unsynced_points'
} as const; 